# ============================================================
# docker-compose.yml — Основной стек selti
# ============================================================
# Использование:
#   docker compose up -d                    # Без Celery workers
#   docker compose --profile dev up -d      # С Flower
# ============================================================

services:
  # ── Selti MCP Server ──────────────────────────────────────
  selti:
    build: .
    container_name: ${SERVICE_NAME}
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file: .env
    environment:
      - SERVICE_NAME=${SERVICE_NAME}
      - MCP_HOST=0.0.0.0
      - MCP_PORT=8000
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      qdrant:
        condition: service_started
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    networks:
      - app-net
    deploy:
      resources:
        limits:
          memory: 512M

  # ── Celery Worker ─────────────────────────────────────────
  celery-worker:
    build: .
    container_name: ${SERVICE_NAME}-worker
    restart: unless-stopped
    command: >
      celery -A memory_server.celery_app worker
      -Q memory,batch,hash
      -c 4
      --without-gossip
      --without-mingle
      --without-heartbeat
      --max-tasks-per-child=1000
      --max-memory-per-child=200000
      -l INFO
      --logfile=-
    env_file: .env
    environment:
      - SERVICE_NAME=${SERVICE_NAME}
      - CELERY_BROKER_URL=redis://redis:6379/0
      - CELERY_RESULT_BACKEND=redis://redis:6379/0
      - DATABASE_URL=postgresql+asyncpg://svc_athene_ai:${SELTI_DB_PASSWORD}@postgres:5432/${POSTGRES_DB}
      - QDRANT_URL=http://qdrant:6333
    depends_on:
      redis:
        condition: service_healthy
      postgres:
        condition: service_healthy
      qdrant:
        condition: service_started
    healthcheck:
      test: ["CMD", "celery", "-A", "memory_server.celery_app", "inspect", "ping", "--timeout=10"]
      interval: 30s
      timeout: 15s
      retries: 3
      start_period: 30s
    networks:
      - app-net
    deploy:
      resources:
        limits:
          memory: 512M

  # ── Flower (dev profile) ──────────────────────────────────
  flower:
    build: .
    container_name: ${SERVICE_NAME}-flower
    command: celery -A memory_server.celery_app flower --port=5555
    env_file: .env
    environment:
      - SERVICE_NAME=${SERVICE_NAME}
      - CELERY_BROKER_URL=redis://redis:6379/0
    ports:
      - "5555:5555"
    depends_on:
      redis:
        condition: service_healthy
    profiles:
      - dev
    networks:
      - app-net
    deploy:
      resources:
        limits:
          memory: 128M

  # ── Redis ─────────────────────────────────────────────────
  redis:
    image: redis:7-alpine
    container_name: redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    command: redis-server --appendonly yes
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - app-net
    deploy:
      resources:
        limits:
          memory: 128M

  # ── PostgreSQL ────────────────────────────────────────────
  postgres:
    image: postgres:18
    container_name: postgres
    env_file: .env
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: ${POSTGRES_DB:-memory}
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d ${POSTGRES_DB:-memory}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - app-net
    deploy:
      resources:
        limits:
          memory: 512M

  # ── Qdrant ────────────────────────────────────────────────
  qdrant:
    image: qdrant/qdrant:v1.12.1
    container_name: qdrant
    restart: unless-stopped
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - qdrant-data:/qdrant/storage
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:6333/healthz || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - app-net
    deploy:
      resources:
        limits:
          memory: 256M

# ── Volumes ────────────────────────────────────────────────
volumes:
  redis-data:
  postgres-data:
  qdrant-data:

# ── Networks ───────────────────────────────────────────────
networks:
  app-net:
    driver: bridge

# Production build flags (vite build, mode=production).
# Без этого файла import.meta.env.VITE_SELTI_SETTINGS_MOCK === undefined,
# а useMock() = (flag !== "0") — экран «Конфигурация» уходил бы в мок.
VITE_SELTI_SETTINGS_MOCK=0
